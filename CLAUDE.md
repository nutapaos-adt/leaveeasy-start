# CLAUDE.md

ไฟล์นี้ให้แนวทางแก่ Claude Code (claude.ai/code) เวลาทำงานกับโค้ดในโปรเจกต์นี้

## โปรเจกต์

LeaveEasy — เว็บ prototype ของระบบขอลาออนไลน์ เขียนด้วย HTML/CSS/JS ธรรมดา เป็นใบงานของ
ADT-RAISE Non-Degree Batch 2, Module 2 (สัปดาห์ที่ 6–9) โค้ดถูกออกแบบให้สร้างทีละสัปดาห์แบบค่อยเป็นค่อยไป
โดยตั้งใจ — อย่าเข้าใจว่า "ทำเยอะกว่า/ครบกว่า" คือดีกว่าเสมอ

## กติกาข้อเดียวที่สำคัญที่สุด อยู่เหนือกว่าทุกอย่าง

**อ่าน [leaveeasy-spec.md](leaveeasy-spec.md) ก่อนลงมือทำอะไรทั้งนั้น** เอกสารนี้คือใบสั่งงานจริง ไม่ใช่แค่
เอกสารประกอบ มันระบุไว้ชัดเจน (หัวข้อ 0.1, หัวข้อ 9) ว่า **ให้ทำเฉพาะที่เขียนไว้เท่านั้น ห้ามเพิ่มฟีเจอร์ที่ไม่ได้ระบุ
และห้ามทำงานของสัปดาห์ถัดไปล่วงหน้า** (ดูตารางรายสัปดาห์ในหัวข้อ 8 — เช็กว่าตอนนี้อยู่สัปดาห์ไหนก่อนลงมือสร้าง)
ถ้าสรุปสเปคออกมาแล้วไม่ตรงกับสิ่งที่ถูกขอให้ทำ ให้แก้ไขเอกสารสเปคก่อน อย่าเพิ่งไปแตะหน้าจอ

ชื่อช่องข้อมูล/ชื่อ collection ตัวพิมพ์เล็ก-ใหญ่มีผลจริงโดยตั้งใจ — `status` กับ `Status` ถือเป็นคนละช่องใน
Firestore ซึ่งสเปคเตือนไว้แล้วว่าเป็นกับดักที่ทำให้ระบบพังแบบเงียบ ๆ ให้คัดลอกชื่อจากสเปคมาตรง ๆ
อย่าเดาหรือดัดแปลงเอง

## คำสั่งที่ใช้บ่อย

- ไม่มีขั้นตอน build, bundle, lint หรือ test — ตั้งใจให้เป็นแบบนี้ (ดูสเปคหัวข้อ 9): HTML/CSS/JS ธรรมดา
  ไม่มี framework ไม่มีเซิร์ฟเวอร์ของตัวเอง
- `npm run dev` → `serve -l 3000 .` — เซิร์ฟเวอร์ไฟล์สแตติกธรรมดา `.claude/launch.json` ตั้งค่าไว้เป็น
  launch config ชื่อ `dev` ให้ preview tool ของ Claude Code เรียกใช้อยู่แล้ว ให้ใช้ทางนั้นแทนการรันคำสั่งเอง
- ทุกหน้า รวมถึง `leave-requests.html` เปิดผ่าน `file://` (ดับเบิลคลิก) ได้ตรง ๆ เหมือนเดิม —
  ที่เป็นแบบนี้ได้เพราะ Firebase ถูกโหลดด้วย SDK แบบ **compat** (แท็ก `<script>` ธรรมดา ไม่ใช่ ES module)
  ห้ามเปลี่ยนไปใช้ Firebase SDK แบบ modular/ESM เพราะจะทำให้เปิดผ่าน `file://` ไม่ได้

## สถาปัตยกรรม

เว็บหลายหน้า ไฟล์ `.html` หนึ่งไฟล์ต่อหนึ่งหน้าจอ ไม่มี router ไม่มี bundler ทุกหน้าใช้รูปแบบ `<head>`
เดียวกัน: `css/style.css` ตามด้วย `js/util.js`, `js/nav.js` จากนั้น (เฉพาะหน้าที่แตะ Firestore) แท็ก
`<script>` ของ Firebase compat ตามด้วย `js/firebase-config.js` แล้วปิดท้ายด้วยสคริปต์ของหน้านั้นเอง
`js/<page>.js` — ทุกตัวใส่ `defer` ลำดับการโหลดนี้สำคัญ: `firebase-config.js` สมมติว่าสคริปต์ของ compat
SDK รันไปแล้ว และสคริปต์ของแต่ละหน้าก็สมมติว่า `util.js`/`nav.js` รันไปแล้วเช่นกัน

- `js/nav.js` — ฉีดแถบเมนูด้านบนที่ใช้ร่วมกันลงใน `<div id="nav">` ของทุกหน้า และประกาศฟังก์ชัน
  `showConfigWarning()` แบบ global ไว้ใช้ตอนอ่าน Firestore ไม่สำเร็จ
- `js/util.js` — ตัวช่วยที่ใช้ร่วมกันทุกหน้า: `esc()` (แปลง HTML ให้ปลอดภัยก่อนยัดลง `innerHTML`),
  `ป้ายสถานะ()` (สร้าง markup ป้ายสถานะ), `เวลาตอนนี้()` (เวลาปัจจุบันในรูปแบบเดียวกับข้อมูลตัวอย่าง),
  `ค่าจากURL()` (อ่านค่าจาก query string)
- `js/firebase-config.js` — จุดตั้งค่า Firebase compat ที่เดียวของทั้งระบบ เปิดให้ใช้ `window.db`
  (instance ของ Firestore) สำหรับสคริปต์อื่นทุกตัว
- `js/data.js` — `window.LEAVE_DATA`: ข้อมูลปลอมที่พิมพ์ค้างไว้ มีโครงสร้างตรงกับเอกสารจริงบน Firestore
  เป๊ะ (ชื่อช่องข้อมูลตรงกันโดยตั้งใจ เพื่อให้เอาชุดเดียวกันไปใส่ลง Firestore ได้ตรง ๆ ในภายหลัง)
- `js/seed-firestore.js` (ขับเคลื่อนโดย `seed-firestore.html`) — สคริปต์ทำครั้งเดียว ใส่
  `window.LEAVE_DATA` ลง Firestore โปรเจกต์จริง รวมถึงโฟลเดอร์ย่อย `approvals` ด้วย
- `js/<page>.js` ของแต่ละหน้าเป็นฟังก์ชันที่เรียกตัวเองทันที (self-invoking) ทำงานครั้งเดียวตอนโหลดหน้า

### สถานะการย้ายระบบ — บางหน้าอ่าน Firestore แล้ว บางหน้ายังไม่อ่าน

การแบ่งแบบนี้ตั้งใจให้เป็นไปตามขอบเขตของสัปดาห์ปัจจุบัน ไม่ใช่ความไม่สอดคล้องกันที่ต้อง "แก้ให้ตรงกัน":

- `leave-requests.js` อ่านข้อมูลจริงจากโฟลเดอร์ `leaveRequests` บน Firestore แล้วรวมกับใบที่ยังไม่ถูก sync
  (`sessionStorage["ใบลาที่ยื่นใหม่"]`)
- `new-leave-request.js`, `leave-request-detail.js`, `leave-types.js` ยังแก้ไขแค่
  `window.LEAVE_DATA`/`sessionStorage` เท่านั้น — ยังไม่เขียนลง Firestore ให้เช็กตารางรายสัปดาห์ในสเปค
  (หัวข้อ 8) ก่อนจะไปแก้ตรงนี้

### โครงสร้างข้อมูลบน Firestore (รายละเอียดครบทุกช่องอยู่ในสเปคหัวข้อ 5)

```
users/{id}            { name, email, role: employee|manager|hr }
leaveTypes/{id}        { name }
leaveRequests/{id}     { title, reason, status,
                         requesterId, requesterName,   ← ช่องชื่อคือของที่จดซ้ำ (denormalized)
                         approverId, approverName,
                         leaveTypeId, leaveTypeName,
                         startDate, endDate, createdAt }
  approvals/{id}       (โฟลเดอร์ย่อย) { authorId, authorName, message, createdAt }
```

โฟลเดอร์ (collection) ทั้งหมดที่มีบน Firestore ตอนนี้มี 4 ชื่อ สะกดตามนี้เป๊ะ ๆ (ตัวพิมพ์เล็ก-ใหญ่มีผล):

- `users` — ผู้ใช้ทั้ง 3 บทบาท
- `leaveTypes` — ประเภทการลา
- `leaveRequests` — ใบขอลา (collection หลักของระบบ)
- `leaveRequests/{id}/approvals` — โฟลเดอร์ย่อย (subcollection) ของความเห็นการอนุมัติ ซ้อนอยู่ในแต่ละ
  เอกสารของ `leaveRequests` ไม่ใช่โฟลเดอร์ระดับบนสุด

- ช่อง `*Name` ตั้งใจจดซ้ำไว้ (denormalize) เพราะ Firestore ไม่มี JOIN สเปคยอมรับไว้แล้วว่าข้อมูลนี้อาจ
  เก่าไม่ตรงกับต้นฉบับได้ และตั้งใจไม่ทำระบบไล่อัปเดตอัตโนมัติ — อย่าเพิ่มเข้าไปเองถ้าไม่มีคนสั่ง
- `status` ของใบลามีได้แค่ 3 ค่าเท่านั้น และเปลี่ยนได้ทางเดียว ห้ามย้อนกลับ:
  1. `รอพิจารณา` — ค่าเริ่มต้นของใบลาใหม่ทุกใบ ตั้งให้อัตโนมัติ ไม่ให้ผู้ใช้เลือกเอง
  2. `อนุมัติ` — เปลี่ยนมาจาก `รอพิจารณา` เท่านั้น เป็นสถานะปลายทาง เปลี่ยนต่อไปเป็นค่าอื่นไม่ได้อีก
  3. `ไม่อนุมัติ` — เปลี่ยนมาจาก `รอพิจารณา` เท่านั้น เป็นสถานะปลายทางเช่นกัน
  ตอนเปลี่ยนสถานะให้แก้เฉพาะช่อง `status` เท่านั้น ห้ามเขียนทับทั้งเอกสาร
- การตั้งสถานะเป็น `ไม่อนุมัติ` ต้องมีความเห็นใน `approvals` อย่างน้อย 1 รายการอยู่ก่อนแล้ว —
  บังคับใช้อยู่ใน `leave-request-detail.js`
- ลบใบลาได้เฉพาะตอนที่ `status` ยังเป็น `รอพิจารณา` เท่านั้น

## ธรรมเนียมของโค้ดนี้

- ชื่อตัวแปร/ฟังก์ชัน และคอมเมนต์ในโค้ดใช้ภาษาไทย (`ใบ`, `ความเห็น`, `กล่องเตือน`, ชื่อฟังก์ชันอย่าง
  `แสดงตาราง`) เวลาแก้ไฟล์ที่มีอยู่แล้วให้ทำตามแบบนี้ต่อไป อย่าปนชื่อภาษาอังกฤษเข้าไปกลางไฟล์
- ชื่อช่องข้อมูลบน Firestore และรหัสบทบาท (`employee`/`manager`/`hr`, `title`, `status` ฯลฯ) ให้คงเป็น
  ภาษาอังกฤษตรงตามที่ระบุในสเปคหัวข้อ 2 และ 5 เสมอ — มีแค่ข้อความที่แสดงบนหน้าจอเท่านั้นที่เป็นภาษาไทย
- ข้อความที่ผู้ใช้พิมพ์เข้ามาต้องผ่าน `esc()` ก่อนเอาไปใส่ใน `innerHTML` ทุกครั้ง — ทุกหน้าที่มีอยู่แล้วทำแบบนี้
  หมด ถือเป็นแพตเทิร์นป้องกัน XSS ที่ใช้ประจำในโปรเจกต์นี้ ไม่ใช่ทางเลือก
- ห้ามทำ pagination เด็ดขาด — สเปคหัวข้อ 9 เลื่อนเรื่องนี้ไปทำใน module ถัดไป ให้ใช้การจำกัด/limit
  จำนวนผลลัพธ์ที่ดึงมาแทน

## ข้อห้าม: ห้ามใส่คีย์ลงไฟล์ที่ push ขึ้น GitHub

- **ห้ามใส่ API key, token, หรือ credential ลับใด ๆ ลงในไฟล์ที่จะ commit/push เด็ดขาด** — ไม่ว่าจะเป็นคีย์ของ
  บริการภายนอกที่จะมาใช้ในสัปดาห์ที่ 8 (เช่น OpenRouter สำหรับปุ่มผู้ช่วย AI) หรือคีย์อื่นใดก็ตาม
- ข้อยกเว้นเดียวที่มีอยู่แล้วคือ `apiKey` ของ Firebase Web App ใน `js/firebase-config.js` — ตามคอมเมนต์ใน
  ไฟล์นั้น มันไม่ใช่ความลับ ถูกออกแบบมาให้อยู่ในโค้ดฝั่ง client ได้ตามปกติ สิ่งที่ป้องกันข้อมูลจริงคือ
  Firestore Security Rules ไม่ใช่การซ่อนค่านี้ — อย่าย้ายค่านี้ไปไว้ในไฟล์ที่ไม่ push โดยไม่มีคนสั่ง
  เพราะจะทำให้เปิดผ่าน `file://` พังตามที่อธิบายไว้ในหัวข้อ "คำสั่งที่ใช้บ่อย"
- คีย์ลับตัวอื่น (ที่ไม่ใช่ Firebase Web App apiKey) ต้องไม่ hardcode ลงไฟล์ที่ push — ตอนถึงสัปดาห์ที่ 7
  ผู้เรียนจะเพิ่มกฎใน `.gitignore` เองให้กันไฟล์ที่มีคีย์ (ดูคอมเมนต์ท้าย `.gitignore` ปัจจุบัน)
- ก่อน push ทุกครั้ง ให้ตรวจรายชื่อไฟล์ที่กำลังจะขึ้น GitHub ด้วยตาก่อนเสมอ ถ้าเห็นไฟล์ที่มีคำว่า
  key/secret/config หรือดูน่าสงสัย ให้หยุดและถามผู้ใช้ก่อน อย่าเพิ่ง push

## เอกสารสเปคคู่ขนาน

`leaveeasy-spec.md` หัวข้อ 10 เทียบโครงสร้างของระบบนี้กับระบบสาธิตคู่ขนานชื่อ "FixIt" ที่ผู้สอนใช้ในคาบ
บรรยาย — โครงสร้างเหมือนกันแต่ชื่อช่องข้อมูลต่างกัน (เช่น `tickets` เทียบกับ `leaveRequests`) ไม่เกี่ยวกับ
โค้ดในโปรเจกต์นี้โดยตรง มีประโยชน์แค่ตอนถูกถามให้อธิบายความเทียบเคียงนี้
